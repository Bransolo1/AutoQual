global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "sensehub-api"
    static_configs:
      - targets: ["api:4000"]
  - job_name: "sensehub-web"
    static_configs:
      - targets: ["web:3000"]
